<div class="col-lg-4">
                    <div class="item">
                        <div class="thumb">
                            <div class="hover-content">
                                <ul>
                                    <li><a href="{{ path('article',{id: article.id}) }}"><i class="fa fa-eye"></i></a></li>
                                </ul>
                            </div>
                            <img src="{{ asset('/image/'~(article.Image )) }}" alt="">
                        </div>
                        <div class="down-content">
                            <h4>{{article.nom}}</h4>
                            <span>{{article.prix}}€</span>
                            <span>{{article.description}}</span>
                        </div>
                    </div>
                </div>
                